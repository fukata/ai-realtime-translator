{
  "name": "ai-realtime-translator",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "echo 'Use package-level dev scripts: frontend/worker'",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "deploy": "bash scripts/deploy.sh"
  },
  "workspaces": [
    "frontend",
    "worker"
  ],
  "devDependencies": {
    "wrangler": "^4.33.1"
  }
}
